"use strict";
document.addEventListener('DOMContentLoaded', () => {
	const tooltips = document / querySelectorAll('.has-tooltip');

	tooltips.forEach((element) => {
		element.addEventListener("click", (e) => {
			e.preventDefault();

			const rect = element.getBo
		});
	});
});